version: 1.0.{build}
branches:
  only:
  - master
before_build:
- cmd: nuget restore ./Trouvaille.sln
build:
  project: ./Trouvaille.sln
  publish_nuget: true
  include_nuget_references: true
  verbosity: minimal
